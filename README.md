# Mullen Gallery

A static artist gallery with category filters and a full-size lightbox viewer.

The site is designed for GitHub Pages and requires no build process or dependencies.

## Local preview

Open `index.html` in a browser, or serve this directory with any static web server.

## Publishing

In the repository settings, select **Pages**, choose **Deploy from a branch**, and publish the root of the `main` branch.
